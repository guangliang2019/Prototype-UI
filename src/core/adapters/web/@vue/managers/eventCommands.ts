import { EventCommands } from '@/core/interface/event';
import { EventManager } from '@/core/interface';
export const createEventComment = (eventManager:EventManager): EventCommands => {
  return {
    // TODO: 实现 Vue 的事件管理
  };
}; 